name: Workflows, Jobs, and Steps

on: [push]

jobs:
  job-1-Linux-List-all-files:
    runs-on: ubuntu-24.04
    steps:
      - name: Create a text file and write content
        run: echo "Hello text!" > filename.txt

      - name: List all files in the virtual machine (Linux/macOS)
        run:  |
          pwd
          ls  -ltra && cat filename.txt
  job-2-GitHub-List-all-repo-files:
    runs-on: ubuntu-24.04
    steps:
      
      - name: Checkout Repo
        uses: actions/checkout@v6 #The command uses: actions/checkout@v4 (or v6) is the act of downloading your code from the GitHub servers onto that virtual machine.

      - name: List github repository files
        run: |
          if [ -z "$(ls -A . 2>/dev/null)" ]; then
              echo "null"
          else
              echo "Directory is not empty:"
              ls -la
          fi
  job-3-downloading-code-from-the-GitHub-servers-onto-this-virtual-machine: 
    runs-on: ubuntu-24.04
    steps:
      - name: Hello job-3
        run: echo "A job-3 running..."
  job-say-hello-inline-python: ##
    runs-on: ubuntu-24.04
    steps:
      - name: Hello job-4
        run: print("A job job-say-hello-inline-python from python shel script!")
        shell: python
